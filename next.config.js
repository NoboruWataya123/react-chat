/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', 'res.cloudinary.com', 'avatars.githubusercontent.com', 'https://avatars.githubusercontent.com'],
    }
}

module.exports = nextConfig
